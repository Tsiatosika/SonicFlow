package com.example.sonicflow.presentation.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette de couleurs moderne pour SonicFlow
 * Inspirée des meilleurs lecteurs de musique (Spotify, Apple Music, YouTube Music)
 */

// Couleurs principales - Gradient vibrant
val PrimaryGradientStart = Color(0xFFE94560)      // Rouge vibrant
val PrimaryGradientMiddle = Color(0xFFD946EF)     // Magenta électrique
val PrimaryGradientEnd = Color(0xFF8B5CF6)        // Violet profond

// Accents
val AccentCyan = Color(0xFF06B6D4)                // Cyan moderne
val AccentGreen = Color(0xFF10B981)               // Vert succès
val AccentYellow = Color(0xFFFBBF24)              // Jaune attention

// Backgrounds - Mode sombre premium
val DarkBackground = Color(0xFF0A0A0F)            // Noir profond
val DarkSurface = Color(0xFF161621)               // Gris foncé
val DarkSurfaceVariant = Color(0xFF1E1E2E)        // Gris moyen

// Backgrounds - Mode clair élégant
val LightBackground = Color(0xFFFAFAFA)           // Blanc cassé
val LightSurface = Color(0xFFFFFFFF)              // Blanc pur
val LightSurfaceVariant = Color(0xFFF3F4F6)       // Gris très clair

// Textes
val OnDarkText = Color(0xFFE5E7EB)                // Blanc doux
val OnDarkTextSecondary = Color(0xFF9CA3AF)       // Gris clair
val OnLightText = Color(0xFF111827)               // Noir doux
val OnLightTextSecondary = Color(0xFF6B7280)      // Gris foncé

// Glass effect - Pour les surfaces modernes
val GlassLight = Color(0x1AFFFFFF)                // Blanc translucide
val GlassDark = Color(0x1A000000)                 // Noir translucide